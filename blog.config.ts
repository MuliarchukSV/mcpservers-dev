import type { BlogConfig } from './template/blog.config.ts';

const config: BlogConfig = {
  name: "MCPServers.dev",
  homeTitle: "MCP Server Directory, Tutorials & Protocol Reference | MCPServers.dev",
  description: "Everything about MCP protocol, servers, and ecosystem",
  site: "https://mcpservers.dev",
  language: "en",
  niche: "MCP servers",
  colors: { primary: "#6366f1", accent: "#22d3ee" },
  analytics: { plausibleDomain: "mcpservers.dev" },
};

export default config;
